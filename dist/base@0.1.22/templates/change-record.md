# <Change title>

Decision: docs/decisions/<NNNN>-<slug>.md

## Context

Optional. What this change is and why now. What was decided lives in the decision record; what changed lives in git.

## Verification

The unit, integration, or end-to-end checks that show the change worked, the exact command, and the output it produced. Manual product validation, when it applies, is recorded separately.
