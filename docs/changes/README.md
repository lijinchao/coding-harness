# Change records

One record per change, named `NNNN-<slug>.md`, from `templates/change-record.md`. Each record carries `## Problem`, `## Approach`, and `## Verification`; `./harness doctor` fails a record that is missing one. This index is exempt from the section check.
